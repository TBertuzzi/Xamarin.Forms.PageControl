<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Xamarin.Forms.PageControlSample"
             x:Class="Xamarin.Forms.PageControlSample.MainPage"
             xmlns:control="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
              xmlns:ci="clr-namespace:Xamarin.Forms.PageControl;assembly=Xamarin.Forms.PageControl" 
           Padding="0,20,0,0">

    <StackLayout>
        <Label Text="Exemplo de PageSize" />

        <ci:PageControl ShowIndicator="{Binding ShowIcons}" IndicatorHeight="8" IndicatorWidth="8" UnselectedIndicator="unselected_circle.png" SelectedIndicator="selected_circle.png" Position="{Binding Position}" ItemsSource="{Binding Screenshots}" />
        <ContentView  HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <control:CarouselView ItemsSource="{Binding Screenshots}" Position="{Binding Position, Mode=TwoWay}">
                <control:CarouselView.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding Url}" WidthRequest="30" HeightRequest="30"></Image>
                    </DataTemplate>
                </control:CarouselView.ItemTemplate>
            </control:CarouselView>
        </ContentView>

    </StackLayout>
	

</ContentPage>
